<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="StarterApp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:StarterApp.Views"
    Shell.FlyoutBehavior="Flyout">

    <!-- Login first. CreateItemPage is NOT listed here — it is only Routing.RegisterRoute (same pattern as MainPage) so GoToAsync("CreateItemPage") works. -->
    <ShellContent ContentTemplate="{DataTemplate views:LoginPage}" />

</Shell>
