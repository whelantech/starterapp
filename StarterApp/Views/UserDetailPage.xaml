<ContentPage x:Class="StarterApp.Views.UserDetailPage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             Title="My Profile">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="20">

            <!-- Loading -->
            <ActivityIndicator 
                IsRunning="{Binding IsLoading}" 
                IsVisible="{Binding IsLoading}" />

            <!-- Error -->
            <Label Text="{Binding ErrorMessage}"
                   TextColor="Red"
                   IsVisible="{Binding ErrorMessage, Converter={StaticResource StringToBoolConverter}}" />

            <!-- User Info -->
            <VerticalStackLayout Spacing="12"
                                 IsVisible="{Binding IsLoading, Converter={StaticResource InvertedBoolConverter}}">

                <Label Text="Profile"
                       FontSize="24"
                       FontAttributes="Bold" />

                <Label Text="First Name" FontAttributes="Bold"/>
                <Label Text="{Binding CurrentUser.FirstName}" />

                <Label Text="Last Name" FontAttributes="Bold"/>
                <Label Text="{Binding CurrentUser.LastName}" />

                <Label Text="Email" FontAttributes="Bold"/>
                <Label Text="{Binding CurrentUser.Email}" />

                <Label Text="Items Listed" FontAttributes="Bold"/>
                <Label Text="{Binding CurrentUser.ItemsListed}" />

                <Label Text="Rentals Completed" FontAttributes="Bold"/>
                <Label Text="{Binding CurrentUser.RentalsCompleted}" />

            </VerticalStackLayout>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>